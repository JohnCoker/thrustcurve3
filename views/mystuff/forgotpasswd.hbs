<h1>{{title}}</h1>

{{#each errors}}
<div class="alert alert-danger" role="alert">
{{this}}
</div>
{{/each}}
{{#if isSent}}
<div class="alert alert-success" role="alert">
Email sent to {{email}} with instructions.
</div>
{{/if}}

<p>
If you have forgotten your ThrustCurve.org account password, you can reset it here.
Otherwise you can <a href="{{loginLink}}">log in</a> or <a href="{{registerLink}}">register</a>.
Basic features work without an account, but more is available if you are logged in.
</p>

<form role="form" class="form-horizontal" name="forgot" action="{{submitLink}}" method="post" data-toggle="validator">
  <div class="form-group">
    <label for="username" class="col-sm-3 control-label">Email</label>
    <div class="col-sm-9">
      <input type="email" name="username" class="form-control" value="{{email}}" required />
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-9 form-submit">
      {{#if redirect}}
      <input type="hidden" name="redirect" value="{{redirect}}" />
      {{/if}}
      <button type="submit" class="btn btn-primary">Reset</button>
      <a class="btn btn-default" href="{{loginLink}}">Log In</a>
    </div>
  </div>
</form>
